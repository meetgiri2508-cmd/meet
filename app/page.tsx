'use client';
import {useState} from 'react';
export default function Home(){
 const [url,setUrl]=useState(''); const [status,setStatus]=useState(''); const [clips,setClips]=useState<string[]>([]);
 async function run(){setStatus('Agent is analyzing the source…'); await new Promise(r=>setTimeout(r,900)); setStatus('Finding hooks, cutting clips, generating captions…'); await new Promise(r=>setTimeout(r,900)); setClips(['Hook: The surprising idea nobody talks about','Key insight: the 30-second explanation','Best moment: the big reveal']); setStatus('3 clips ready for review.');}
 return <main style={{minHeight:'100vh',padding:40,fontFamily:'system-ui',background:'linear-gradient(135deg,#0b1020,#16112b)',color:'#fff'}}><div style={{maxWidth:1050,margin:'auto'}}><p style={{opacity:.7}}>CLIPFORGE AI</p><h1 style={{fontSize:52,margin:'10px 0'}}>Long video → viral-ready clips.</h1><p style={{fontSize:19,opacity:.75,maxWidth:700}}>An AI content agent that finds the strongest moments, reframes them vertically, writes captions and prepares multi-platform publishing.</p><section style={{marginTop:35,padding:24,border:'1px solid #ffffff20',borderRadius:22,background:'#ffffff08'}}><label>Paste a YouTube URL or content source</label><div style={{display:'flex',gap:12,marginTop:12}}><input value={url} onChange={e=>setUrl(e.target.value)} placeholder='https://youtube.com/watch?v=...' style={{flex:1,padding:16,borderRadius:12,border:'1px solid #ffffff20',background:'#080b15',color:'#fff'}}/><button onClick={run} style={{padding:'14px 22px',border:0,borderRadius:12,fontWeight:700}}>Create clips</button></div><p style={{opacity:.65}}>{status}</p></section><div style={{display:'grid',gridTemplateColumns:'repeat(3,1fr)',gap:16,marginTop:24}}>{clips.map((c,i)=><article key={c} style={{padding:20,borderRadius:18,border:'1px solid #ffffff18',background:'#ffffff08'}}><div style={{height:210,borderRadius:14,background:'linear-gradient(160deg,#252b45,#090b12)',display:'grid',placeItems:'center',fontSize:40}}>▶</div><h3>{c}</h3><small>9:16 • Auto captions • Hook score {91-i*7}/100</small><button style={{marginTop:15,padding:10,borderRadius:10,border:'1px solid #ffffff22',background:'transparent',color:'#fff'}}>Schedule / Publish</button></article>)}</div><section style={{marginTop:28,display:'flex',gap:12}}><span>✓ YouTube Shorts</span><span>✓ TikTok</span><span>✓ Captions</span><span>✓ Auto titles</span></section></div></main>}
